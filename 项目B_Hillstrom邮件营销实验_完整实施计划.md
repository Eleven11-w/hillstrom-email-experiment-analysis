# 项目 B：Hillstrom 电商邮件营销增量实验完整实施计划

> 版本：v1.1  
> 建立日期：2026-09-08  
> 最后复核：2026-09-08  
> 项目定位：数据分析实习生作品集中的 A/B 实验与增量评估项目  
> 目标仓库名：`hillstrom-email-ab-test`  
> 预计工期：8–10 天，共 25–35 小时

---

## 0. 一句话目标

使用 Hillstrom 公开的三组随机邮件实验数据，评估“男装邮件”和“女装邮件”相对于“不发邮件”是否带来两周内的增量访问、转化和客均销售额，并形成可复现、可解释、可承受面试追问的业务决策报告。

---

## 1. 项目边界与诚信声明

### 1.1 本项目证明什么

- 能将营销问题转成可检验的实验假设。
- 能区分业务指标、主要估计量、辅助指标和探索性分析。
- 能完成样本比例失衡、实验前平衡和数据逻辑检查。
- 能报告效应量、95% 置信区间、p 值、MDE/精度与多重比较处理。
- 能将统计证据转换为“发不发、发哪种、优先向谁发”的有边界建议。
- 能用 README 中的固定命令重新生成全部结果。

### 1.2 本项目不声称什么

- 不声称自己设计或上线了原始历史实验。
- 不将本项目写成真实“预注册”或“盲态分析”。数据和结果已公开，且执行者可能已接触公开结果线索；本项目只能在运行自己的组间分析前冻结一份**历史实验再分析协议**。
- 不将邮件随机分配等同于邮件已打开或已阅读。主分析是意向治疗效应（ITT）。
- 不把销售额当成利润。数据缺少毛利率、投递成本、退货和长期客户价值。
- 不把事后挑出的显著子组宣称为已确认的定向策略。

---

## 2. 数据来源与字段

### 2.1 一手来源

- 原始发布页：<https://blog.minethatdata.com/2008/03/minethatdata-e-mail-analytics-and-data.html>
- 本次实际下载的发布方 CSV：<http://www.minethatdata.com/Kevin_Hillstrom_MineThatData_E-MailAnalytics_DataMiningChallenge_2008.03.20.csv>
- 发布方说明：数据约含 64,000 名过去 12 个月内购买过的客户；约三分之一随机分配到男装邮件、女装邮件和不发邮件三组；随后跟踪两周。
- 原发布页明确邀请公开分析，但未在页面中给出可核对的正式数据许可证条款；因此默认不在 GitHub 二次分发原始 CSV。

#### 2.1.1 已验证的本地基线

- 本地文件：`data/raw/hillstrom.csv`
- 数据行数：64,000（不含表头）
- 列数：12
- 文件大小：3,964,977 字节
- SHA-256：`0E5893329D8B93CEFECC571777672028290AB69865718020C78C7284F291AECE`
- 三组样本数：Mens E-Mail 21,307；No E-Mail 21,306；Womens E-Mail 21,387。
- HTTPS 直链在本次下载时证书验证失败，未跳过证书验证；改用发布方 HTTP 直链。详见 `data/README.md`。

### 2.2 原始字段及角色

| 字段 | 含义 | 时序角色 | 用途 |
|---|---|---|---|
| `recency` | 距最近一次购买的月数 | 实验前 | 平衡检查、探索性分层 |
| `history_segment` | 过去一年消费额分箱 | 实验前 | 平衡检查、图表 |
| `history` | 过去一年消费额 | 实验前 | 平衡检查、回归精度改善 |
| `mens` | 过去一年是否购买男装 | 实验前 | 品类偏好分层 |
| `womens` | 过去一年是否购买女装 | 实验前 | 品类偏好分层 |
| `zip_code` | Urban/Surburban/Rural（源文件拼写） | 实验前 | 平衡检查、探索性分层 |
| `newbie` | 是否为过去 12 个月新客 | 实验前 | 新老客分层 |
| `channel` | 历史购买渠道 | 实验前 | 平衡检查、探索性分层 |
| `segment` | Mens E-Mail/Womens E-Mail/No E-Mail | 随机分配 | 处理组标记 |
| `visit` | 后续两周是否访问 | 实验后 | 辅助效果指标 |
| `conversion` | 后续两周是否购买 | 实验后 | 辅助效果指标 |
| `spend` | 后续两周销售额 | 实验后 | 主要业务效果指标 |

### 2.3 数据管理规则

- 原始 CSV 保存到 `data/raw/`，脚本只读，不覆盖。
- `data/raw/` 默认不提交 Git；README 保留来源、下载日期、字节大小和 SHA-256。
- 中间数据由脚本生成到 `data/processed/`，不手工修改。
- 已确认原始数据没有客户 ID，必须在局限中说明“无法独立验证每行必然对应唯一客户”；不得用整行去重替代用户去重。
- 原始值 `Surburban` 只在处理层映射为 `Suburban`，原始 CSV 不改。

---

## 3. 业务问题、估计量与决策规则

### 3.1 主要业务问题

1. 男装邮件相对于不发邮件，是否创造了两周客均增量销售额？
2. 女装邮件相对于不发邮件，是否创造了两周客均增量销售额？
3. 男装邮件与女装邮件之间的效果是否有值得关注的差异？
4. 对于哪些客户群，哪种邮件可能更值得进一步验证？

`Mens E-Mail`/`Womens E-Mail` 指邮件推荐的商品类型，不代表客户性别。

### 3.2 分析单位和处理原则

- 分析单位：被随机分配的客户。
- 处理定义：以 `segment` 的随机分配为准，无论邮件是否打开。
- 分析原则：ITT，不使用实验后的 `visit`/`conversion` 筛选样本。
- 时间窗：邮件后两周，按原始数据定义。

### 3.3 主要估计量

| 编号 | 估计量 | 差异方向 |
|---|---|---|
| P1 | `mean(spend | Mens E-Mail) - mean(spend | No E-Mail)` | 正数代表男装邮件增加客均销售额 |
| P2 | `mean(spend | Womens E-Mail) - mean(spend | No E-Mail)` | 正数代表女装邮件增加客均销售额 |

两个主要比较共享 family-wise `alpha=0.05`，对两个 p 值做 Holm 校正。主报告以未调整的效应量和**单个对比的边际 95% CI**为核心，同时报告调整后 p 值；不将边际 95% CI 误称为同时覆盖两个对比的区间。

次要直接对比 S1：`mean(spend | Mens E-Mail) - mean(spend | Womens E-Mail)`，用于回答“两个邮件版本哪个更好”。S1 报告效应量和边际 95% CI，标记为次要分析，不改变 P1/P2 的主要决策族。

### 3.4 辅助指标

- 访问率：`sum(visit) / n`。
- 转化率：`sum(conversion) / n`。
- 付费客单价：`sum(spend) / sum(conversion)`，只做描述性分解。它条件于实验后转化，组间比较可受选择偏差影响，不作因果结论。
- 仅当数据 QC 确认 `conversion=1` 均满足 `visit=1` 时，才将“分配客户 → 访问 → 转化”称为漏斗；否则分别报告两个结果率。不虚构数据中没有的打开/点击环节。
- 辅助指标和 S1 不用于替换主要结论；其 CI/p 值按探索性、名义水平解读，不将“至少一个显著”作为决策规则。

### 3.5 业务显著性与决策规则

数据不含投递成本、毛利率和退货，因此不设一个伪精确的“盈利 MDE”。使用两层表述：

1. **证据层**：报告效应方向、CI、经 Holm 校正的 p 值和当前样本精度。
2. **决策层**：使用 `每位分配客户增量贡献 = ΔSpend × 毛利率 - 每位分配客户邮件成本` 做场景敏感性。毛利率与投递成本均使用多组明示假设，不伪装成数据中的真实值；同时报告盈亏平衡所需的 `ΔSpend = 邮件成本 / 毛利率`。

建议上线前还需补充毛利、退货、退订/投诉、邮件投递成本和长期客户价值。

---

## 4. 拟建项目结构

```text
hillstrom-email-ab-test/
├─ README.md
├─ requirements.txt
├─ .gitignore
├─ data/
│  ├─ README.md
│  ├─ raw/                 # 不提交原始 CSV
│  └─ processed/           # 脚本生成
├─ docs/
│  ├─ analysis_plan.md      # 查看组间结果前冻结
│  ├─ data_dictionary.md
│  ├─ metric_definitions.md
│  ├─ report.md
│  ├─ interview_qa.md
│  └─ resume_evidence.md
├─ src/
│  ├─ run_all.py
│  ├─ validate_data.py
│  ├─ analyze_experiment.py
│  ├─ analyze_segments.py
│  └─ make_figures.py
├─ tests/
│  ├─ test_validation.py
│  └─ test_estimands.py
└─ results/
   ├─ tables/
   ├─ figures/
   └─ run_manifest.json
```

不在第一版中增加 notebook、Web 应用、Docker、数据库或机器学习服务。项目主线用可测试的 Python 脚本完成。

---

## 5. 详细实施步骤

### 阶段 B0：建立环境和数据证据（0.5 天）

#### 操作

1. 创建仓库和上述最小目录。
2. 创建隔离 Python 环境，依赖仅保留 `pandas`、`numpy`、`scipy`、`statsmodels`、`matplotlib`、`seaborn`、`pytest`。
3. 复核已下载 CSV 的下载日期、字节大小和 SHA-256；运行时输入哈希不一致必须失败。
4. 仅查看列名、类型、总行数与非结果字段的合法值；在冻结再分析协议前，不运行自己的按 `segment` 效果分析。

#### 验收

- [x] Python 导入命令全部成功。
- [x] `data/README.md` 有来源 URL、下载日期、大小和 SHA-256。
- [x] 原始数据只读且被 `.gitignore` 忽略。
- [x] 已在项目日志披露数据和外部结果是否曾被查看，不声称盲态。

### 阶段 B1：冻结再分析协议（0.5–1 天）

#### 操作

1. 在 `docs/analysis_plan.md` 写明：
   - 业务问题与限定的时间窗；
   - 三组处理定义与 ITT 原则；
   - P1/P2 主要估计量与差异方向；
   - 主要、辅助和探索性指标；
   - S1 男装邮件 vs 女装邮件的次要直接对比；
   - 双侧检验、family-wise `alpha=0.05`、Holm 校正；
   - 缺失、异常和逻辑冲突的处理规则；
   - Bootstrap 随机种子、重复次数和 CI 方法；
   - 计划图表、分层与决策规则。
2. 对该文件生成 SHA-256，并在 `run_manifest.json` 记录。
3. 冻结后如必须修改，追加“修订日志”，不静默覆盖。

#### 验收

- [ ] 协议如实披露已知的外部结果线索，且未根据本次自行分析的组间结果调整指标或阈值。
- [ ] 主要效应的方向唯一、无歧义。
- [ ] 能用 60 秒说清为什么用客均 Spend 而不是只看转化率。
- [ ] 文件哈希已记录。

### 阶段 B2：数据质量与实验完整性（1 天）

#### 操作

1. 输出行数、列数、类型、缺失数、非法类别和数值范围。
2. 检查 `visit`/`conversion` 是否只取 0/1，`spend >= 0`。
3. 检查逻辑关系：例如 `conversion=0` 但 `spend>0`、`conversion=1` 但 `spend=0`、`conversion=1` 但 `visit=0`；不自动删除，先列出数量和可能解释。
4. 用卡方拟合优度检验三组是否偏离预期 1:1:1 分配，报告组别数量、比例和 SRM p 值。
5. 对实验前连续变量报告三个两两对比的 standardized mean difference（SMD）及其最大绝对值，对类别变量报告各组占比和最大绝对差。`|SMD| > 0.10` 只触发审查和调整稳健性分析，不自动删除数据或否定发布方的随机分配说明。
6. 产生 `results/tables/data_qc.csv`、`srm_check.csv` 和 `balance_check.csv`。

#### 验收

- [ ] 无未解释的非法类别、负 Spend 或核心字段缺失。
- [ ] SRM 检验结果已报告；若 `p<0.01`，停止因果主结论并先审计分流/数据来源。
- [ ] 实验前平衡性用效应量描述，没有把“每个 p 值都不显著”误当成随机化证明。
- [ ] 所有异常的保留/排除决定有规则和记录。

### 阶段 B3：描述统计与主分析（1–1.5 天）

#### 操作

1. 三组分别输出：
   - 样本量；
   - 访问人数与访问率；
   - 转化人数与转化率；
   - 总 Spend、全样本客均 Spend；
   - Spend 中位数、P90、P95、P99 和最大值。
2. 对 P1/P2 计算：
   - 未调整均值差；
   - 固定种子的组内客户级 Bootstrap 95% percentile CI；
   - 双侧 Welch 均值差检验提供两个主要 p 值，Welch 区间与 Bootstrap 区间交叉验证；
   - Holm 调整 p 值。
3. 对 S1 计算男装邮件 - 女装邮件的 Spend 均值差和边际 95% CI，标记为次要直接比较。
4. 转化率和访问率分别报告绝对百分点差、相对差异、边际 95% CI 和两比例检验结果，统一标记为辅助/探索性。
5. 生成主结果表 `experiment_effects.csv`，固定列：`analysis_tier, contrast, metric, n_treatment, n_control, mean_treatment, mean_control, absolute_effect, relative_effect, ci_level, ci_scope, ci_low, ci_high, p_raw, p_adjusted, method`。

#### 图表

- 图 1：三组访问率、转化率与 95% CI。
- 图 2：两个主要 Spend 效应的点估计和 95% CI 森林图。
- 图 3：Spend 的零值比例与正值长尾分布，使用对数坐标或 ECDF，不只画普通均值柱状图。

#### 验收

- [ ] 所有效应方向一致为“邮件组 - 空白控制组”。
- [ ] 主表同时包含效应量、CI 和 p 值，不只写“显著/不显著”。
- [ ] Python 库计算与至少一项手工公式/独立实现一致。
- [ ] Bootstrap 在同一环境重跑得到完全一致的输出。
- [ ] 没有因为结果不显著而更换主指标或删除样本。

### 阶段 B4：精度、MDE 与稳健性（1 天）

#### 操作

1. 使用实际样本量和空白组基线转化率，报告在双侧 `alpha=0.05`、`power=0.80` 下转化率可检测的绝对百分点差，并明确它是“设计/精度参考”，不报告基于观察效应的事后 power。
2. 对 Spend 优先报告实际 CI 宽度和决策阈值敏感性；如果进行模拟 MDE，必须保留零膨胀和长尾特性，并将模拟假设写入报告。
3. 使用实验前变量做协变量调整的稳健性分析：
   - Spend：OLS + heteroskedasticity-robust SE；
   - Visit/Conversion：线性概率模型 + robust SE 用于百分点效应；
   - 协变量仅从实验前允许列表取值；`history` 与由它派生的 `history_segment` 默认只选一种表达，避免重复信息和不必要的共线性；
   - 处理效应解读仍以随机分配为基础，调整的目的是提高精度。
4. 比较未调整和协变量调整效应的方向、大小和 CI。

#### 验收

- [ ] 对“样本够不够”的回答同时包含 alpha、power、基线率和 MDE/CI 精度。
- [ ] 不使用“检验不显著，所以两组相同”的表述。
- [ ] 调整分析和未调整 ITT 结果都保留，不挑更显著的版本。
- [ ] Spend 的长尾性与零值已在方法和局限中说明。

### 阶段 B5：探索性客户分层（1–1.5 天）

#### 范围

仅优先分析三类实验前特征，防止无限切分子组：

1. 历史品类偏好：`mens`/`womens`。
2. 客户阶段：`newbie`。
3. 历史渠道：`channel`。

`recency` 和 `history` 可用预先固定的分位数分箱做图，但不额外扩大确认性结论数量。

#### 方法

- 先报告子组的样本量、效应量和 CI。
- 使用“处理 × 实验前特征”交互项直接检验异质性，不用“一个子组显著、另一个不显著”代替交互检验。
- 对分层结果统一标记“探索性”，只生成后续实验假设。
- 不在主线中训练 uplift 机器学习模型。只有前述 Gate 全部通过且时间有余，才可建立独立扩展分支。

#### 验收

- [ ] 每个子组建议都能追溯到一个实验前字段。
- [ ] 无任何使用 Visit/Conversion/Spend 定义的事后人群。
- [ ] 报告明确区分“已验证的总体效果”与“待下一次实验验证的分层线索”。
- [ ] 不输出虚假的精确“最佳 10,000 人”名单；无 ID 时只能描述可执行的人群规则。

### 阶段 B6：报告、复现和测试（1–1.5 天）

#### 报告结构（3–5 页等价内容）

1. 执行摘要：最多 5 条，结果先行。
2. 业务问题与决策选项。
3. 数据、随机分配说明和质量检查。
4. 估计量、检验、CI 和多重比较。
5. 主要结果与证据强度。
6. 访问 → 转化 → Spend 的机制解读。
7. 探索性客户分层与下一次实验建议。
8. 局限、所需补充数据与业务决策。

#### README 必须包含

- 项目背景、数据来源和诚信边界。
- 环境安装和唯一推荐执行顺序。
- 一条全量复现命令，例如 `python -m src.run_all`（实施时以真实入口为准）。
- 精确的 Python 版本和直接依赖版本，不使用无上限的模糊依赖。
- 预期数据行数、三组样本数和最终主要效应基线。
- 输出文件索引和三张主图。
- 方法、业务结论、局限和简历表述。

#### 至少测试

- [ ] 非法 `segment` 值会使验证失败。
- [ ] 非 0/1 的 `visit`/`conversion` 会使验证失败。
- [ ] 负 `spend` 会使验证失败。
- [ ] 人工构造的小数据能得到预期的均值差和比例差。
- [ ] 效应方向始终为 treatment - control。
- [ ] Holm 校正输出与 `statsmodels` 结果一致。
- [ ] 固定种子后 Bootstrap 可重现。
- [ ] 协变量/分层设计矩阵中出现 `visit`、`conversion` 或 `spend` 时测试必须失败，防止实验后泄漏。
- [ ] 输入文件哈希或列集改变时，主入口会明确失败。
- [ ] 运行清单完整记录输入 SHA-256、协议 SHA-256、Python/依赖版本、随机种子、运行时间和 Git commit（如已初始化仓库）。

#### 验收

- [ ] `pytest` 全部通过。
- [ ] 在删除 `processed/` 和 `results/` 后，按 README 可重新生成全部结果。
- [ ] 重跑的主要数字与 README 冻结基线一致。
- [ ] 报告中的所有数字均能定位到 CSV/图表或脚本输出。
- [ ] `git status` 不包含原始数据、环境文件夹或临时缓存。

### 阶段 B7：简历与面试验收（0.5–1 天）

#### 项目名称

**电商邮件营销增量实验与客户分层分析**

#### 简历 bullet 模板（只能用真实脚本结果替换占位符）

> - 基于 Hillstrom 公开的约 X 万名客户三组随机邮件历史数据，制定冻结再分析协议，完成 SRM、实验前协变量平衡、数据逻辑与统计精度检查。
> - 以两周客均增量销售额为主要效果，采用客户级 Bootstrap、比例检验与 Holm 多重比较校正，估计男/女装邮件相对空白组的效应（X，95% CI [a, b]），并结合历史品类偏好和渠道形成待二次实验验证的定向策略。

#### 禁止出现的表述

- “独立设计并上线了该实验”。
- “对该公开历史数据完成了真实预注册或盲态分析”。
- “证明邮件必然提升销售”。
- “p>0.05，证明两组没有差异”。
- “收入提升等于利润提升”。
- “定位出最佳的精确 10,000 名客户”（尤其在无 ID 时）。
- 任何无法从冻结脚本输出中复算的数字。

#### 5 分钟讲解结构

1. 30 秒：业务问题和三组实验。
2. 45 秒：主要估计量、ITT 和决策阈值。
3. 45 秒：SRM、平衡与数据质量。
4. 90 秒：效应量、CI、多重比较和精度。
5. 60 秒：访问—转化—Spend 机制解读。
6. 45 秒：分层线索与业务建议。
7. 30 秒：限制与下一次实验。

#### 必答追问

1. 为什么主指标用客均 Spend，不只用 Conversion？
2. 为什么要做 ITT，不只分析实际访问者？
3. SRM 检查什么？发现 SRM 后怎么做？
4. 随机实验为什么还要看实验前平衡？
5. 为什么有两个主要比较？Holm 校正做了什么？
6. Spend 零值多、长尾，为什么还要估计均值？
7. p 值和 95% CI 分别说明什么？
8. 当前样本量够不够？MDE 需要哪些输入？
9. 一个子组显著、另一个不显著，能否说效果异质？
10. 为什么现在不能直接计算 ROI 或净利润？
11. 数据是 2008 年的，对现在业务有什么价值？
12. 如果重新运行实验，你会补哪些指标和护栏？

---

## 6. 总验收门（Gate B）

项目只有在下列 5 个 Gate 全部通过后，才能写入简历。

### Gate B-Data：数据可信

- [ ] 一手来源、下载日期、大小、哈希已记录。
- [ ] 数据类型、缺失、取值、逻辑冲突已检查。
- [ ] 三组样本数和 SRM 结果已报告。
- [ ] 实验前特征平衡性已用效应量描述。
- [ ] 数据无 ID 等限制已如实说明。

### Gate B-Stats：统计正确

- [ ] 历史实验再分析协议在本次组间效果分析前冻结，有哈希/修订记录，并披露非盲态边界。
- [ ] 两个主要对比、效应方向、双侧 alpha 和 Holm 校正已预先定义。
- [ ] 主结果有效应量、95% CI 和调整后 p 值。
- [ ] Bootstrap 可复现，且有独立方法交叉验证。
- [ ] MDE/精度解读没有倒果为因或把不显著当成无效果证明。

### Gate B-Business：业务可用

- [ ] 每个建议都指向一个决策，并有直接数据证据。
- [ ] 区分销售额、毛利和净利润。
- [ ] 对业务阈值做假设敏感性，不伪造投递成本。
- [ ] 总体结论与探索性子组建议已分开。
- [ ] 明确列出真实上线前需补充的指标和二次实验。

### Gate B-Repro：可复现

- [ ] 干净环境按 README 唯一推荐顺序可重跑。
- [ ] 一条入口命令生成 QC、结果表和所有图。
- [ ] 主入口先校验输入哈希和 schema，再运行分析。
- [ ] `run_manifest.json` 记录输入、方案、环境、种子和代码版本。
- [ ] 测试全部通过。
- [ ] 主要数字与 README 基线一致。
- [ ] 原始数据、密钥、环境和缓存未进入 Git。

### Gate B-Resume：可写简历

- [ ] 简历只有 2 条 bullet，每个数字都能复算。
- [ ] 不写“上线实验”、“真实预注册”、“利润提升”等无证据声明。
- [ ] 5 分钟讲解不超时，12 个必答追问可在每题 60 秒内回答。
- [ ] 能现场定位主结果 CSV、分析代码和报告对应段落。
- [ ] GitHub README 的项目背景、方法、结论和局限可在 3 分钟内读完。

---

## 7. 建议时间表

| 天 | 任务 | 当日交付 | 离开当日前的检查 |
|---|---|---|---|
| D1 | B0：环境、已下载数据的来源/哈希/字段复核 | `data/README.md`、环境文件 | 披露已知信息，未运行本次组间效果分析 |
| D2 | B1：冻结历史实验再分析协议 | `analysis_plan.md` | 方向、主指标、S1、Holm、异常规则完整 |
| D3 | B2：QC、SRM、平衡检查 | 3 张 QC 表 | Gate B-Data 通过 |
| D4 | B3：主分析和辅助指标 | 主结果表、主图 | 手算/库计算交叉一致 |
| D5 | B4：精度、MDE、协变量调整 | 稳健性表 | 未调整和调整结果都保留 |
| D6 | B5：预定义探索性分层 | 子组图、互动检验表 | 没有事后挑选成主结论 |
| D7 | B6：报告、README、测试 | 报告、全量复现入口 | Gate B-Stats/B-Repro 通过 |
| D8 | B7：简历证据、追问表、5 分钟录音 | 2 条 bullet、QA、录音 | Gate B-Business/B-Resume 通过 |
| D9 | 从零复现、口径核对、报告修订 | 候选冻结版 | 主数字、图、报告引用一致 |
| D10（缓冲） | 仅修复 Gate 失败项，不新增分析 | 最终冻结版 | 回读全部交付，简历数字最后复算 |

---

## 8. 失败处理与裁剪顺序

### 8.1 数据源不可用

- 不从来源不明的二次 CSV 镜像直接开始。
- 如必须使用镜像，先将列、行数、字节大小、哈希与可获得的一手描述交叉核对，并在 README 说明。

### 8.2 SRM 或严重数据问题

- 停止因果结论。
- 保留数据审计报告，不为了完成简历而放宽 Gate。
- 只有在问题被可验证地解释或修复后才继续。

### 8.3 主结果不显著或不支持邮件

- 不改主指标、不换单侧检验、不删异常子组。
- 报告 CI 排除了哪些效果、尚兼容哪些效果。
- 将“不发送可能更好”作为合格的业务结论。

### 8.4 时间不足时的裁剪顺序

1. 保留：来源证据、冻结再分析协议、QC/SRM、P1/P2、S1、CI、Holm、README 复现、报告。
2. 其次保留：Visit/Conversion 辅助指标、MDE/精度。
3. 可裁剪：协变量调整、大部分客户分层。
4. 最先删除：uplift 机器学习、额外仪表板、Web 应用。

---

## 9. 项目 A/B/C 在简历中的分工

| 项目 | 主证据 | 避免重复 |
|---|---|---|
| A：Olist 经营分析 | MySQL 多表取数、指标体系、经营洞察 | 不再强调普通 Python EDA |
| B：Hillstrom 增量实验 | 随机实验、ITT、效应量/CI、多重比较、业务决策 | 不额外做通用 BI 看板，不把项目扩成算法竞赛 |
| C：电商 BI 看板 | 可视化交付、交互筛选、业务沟通 | 不重复 A 的全部取数和建模 |

简历项目顺序根据目标 JD 调整：通用数据分析岗默认 A → B → C；增长/用户运营岗可改为 B → A → C。

---

## 10. 最终完成定义

“代码跑通”不等于项目完成。本项目完成必须同时满足：

1. **数据可信**：来源、哈希、QC、SRM 和平衡检查齐全。
2. **统计可辩护**：方案先冻结，效应量/CI 为主，处理多重比较，不追逐显著性。
3. **业务有边界**：说清销售额不等于利润，建议与假设分开，指出下一步数据和实验。
4. **工程可复现**：一条命令生成全部核心输出，测试全绿，README 冻结基线数字一致。
5. **简历可追问**：2 条 bullet 无夸大，5 分钟可讲，12 个核心问题可答，所有数字可定位。

只有上述五项同时通过，才将项目 B 标记为“已完成”并写入正式简历。
